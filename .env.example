# Turso Database Configuration
# You must provide these for the scraper to run in production mode
# Otherwise it will fall back to offline mode and skip storing data
TURSO_DB_URL="libsql://your-database-name.turso.io"
TURSO_AUTH_TOKEN="your-turso-auth-token-here"

# Set to "development" to enable debug logging
ENVIRONMENT="production"
